/**
 * Loading component
 * @returns JSX.Element
 *
 * @example
 * ```tsx
 * <Loading />
 * ```
 */
export const Loading = () => {
  return (
    <span className="loading loading-infinity loading-lg"></span>
  )
}
